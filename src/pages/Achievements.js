import React from 'react';
import Header from '../components/Header';

const Achievements = () => {
    return (
        <div>
            <Header />
        </div>
    );
};

export default Achievements;